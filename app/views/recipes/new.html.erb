<h1>Create A New Recipe!</h1>

    <%= render "form" %>

    <% flash.each do |type, msg| %>
        <div>
          <%= msg %>
        </div>
      <% end %>
<h2><%= link_to "Homepage", root_path(@user)%></h2>