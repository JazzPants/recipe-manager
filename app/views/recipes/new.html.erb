<h1>Create A New Recipe!</h1>
<p>User: <%= @user.name%></p>

    <%= render "form" %>

    <p><%= @ingredient %></p>

    <% flash.each do |type, msg| %>
        <div>
          <%= msg %>
        </div>
      <% end %>

<h2><%= link_to "Homepage", root_path(@user)%></h2>